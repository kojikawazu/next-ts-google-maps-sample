import React from 'react';
import GoogleMap from '@/components/GoogleMap';

const Home: React.FC = () => {
  return (
    <div>
      <h1>Google Maps with Next.js and TypeScript</h1>
      <GoogleMap />
    </div>
  );
};

export default Home;